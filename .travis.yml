language: python
python:
  - "2.7"
  - "3.5"
install:
  - pip install --use-mirrors -r requirements.txt
  - pip install --use-mirrors -r requirements-tests.txt
  - python setup.py develop
script: nosetests
